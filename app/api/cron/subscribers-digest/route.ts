import { NextResponse } from 'next/server';import { adminFirestore } from '@/lib/firebase-admin';export const runtime='nodejs';export const maxDuration=300;function auth(r:Request){const s=process.env.CRON_SECRET;return Boolean(s)&&r.headers.get('authorization')===`Bearer ${s}`;}function ms(v:any){if(!v)return 0;if(typeof v.toMillis==='function')return v.toMillis();if(typeof v.toDate==='function')return v.toDate().getTime();return new Date(v).getTime()||0;}function c(v:any){return `"${String(v??'').replace(/"/g,'""')}"`;}async function send(subject:string,url:string,count:number){const key=process.env.RESEND_API_KEY;if(!key)throw new Error('RESEND_API_KEY is not configured.');const from=process.env.TRANSACTIONAL_EMAIL_FROM||'Aureon Music Group <info@aureonmusicgroup.com>';const x=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({from,to:['info@aureonmusicgroup.com'],subject,text:`Daily subscriber report. ${count} subscriber records changed/created in the previous 24 hours. Download: ${url}`,html:`<div style="background:#050505;padding:32px;color:#f5f1e8;font-family:Arial"><h1>${subject}</h1><p>${count} subscriber records created or updated in the previous 24 hours.</p><a href="${url}" style="padding:14px 22px;background:#c6a34f;color:#080808;text-decoration:none;font-weight:700">Download spreadsheet</a></div>`})});if(!x.ok)throw new Error(await x.text());}export async function GET(r:Request){if(!auth(r))return NextResponse.json({error:'Unauthorized'},{status:401});const now=Date.now(),since=now-86400000,date=new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Dublin',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(now));const snap=await adminFirestore.collection('members').limit(10000).get();const members=snap.docs.map(d=>({id:d.id,...d.data()} as any)).filter(m=>ms(m.updatedAt||m.createdAt||m.subscriptionUpdatedAt)>=since);const rows=[['Name','Email','Plan','Subscription status','Stripe customer','Stripe subscription','Cancel at period end','Period end','Created/updated']];for(const m of members)rows.push([m.name||m.fullName||'',m.email||'',m.plan||'',m.subscriptionStatus||m.status||'',m.stripeCustomerId||'',m.stripeSubscriptionId||'',m.cancelAtPeriodEnd?'Yes':'No',m.currentPeriodEnd?.toDate?.()?.toISOString?.()||'',new Date(ms(m.updatedAt||m.createdAt||m.subscriptionUpdatedAt)).toISOString()]);const subject=`Daly Subs -${date}.csv`,ref=adminFirestore.collection('fulfilmentDigests').doc();await ref.set({createdAt:new Date(),csv:rows.map(x=>x.map(c).join(',')).join('\n'),permanent:true,fileName:subject,reportType:'subscribers'});const base=(process.env.NEXT_PUBLIC_SITE_URL||'https://www.aureonmusicgroup.com').replace(/\/$/,'');await send(subject,`${base}/api/fulfilment-digest/${ref.id}`,members.length);return NextResponse.json({ok:true,subscribers:members.length,sent:true});}