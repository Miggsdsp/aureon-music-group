'use client';

import { useEffect, useMemo, useState } from 'react';
import { addDoc, collection, deleteDoc, doc, onSnapshot, serverTimestamp, updateDoc } from 'firebase/firestore';
import { getDownloadURL, ref, uploadBytesResumable } from 'firebase/storage';
import { firestore, firebaseStorage } from '@/lib/firebase-client';
import { artists as builtInArtists } from '@/data/artists';
import { AUREON_STREAM_FORMAT, buildAacStreamingFile } from '@/lib/audio-stream-encoder';
import { AdminShell } from './AdminShell';

type RecordData = { id:string; title?:string; name?:string; slug?:string; description?:string; status?:string; price?:number; featured?:boolean; details?:Record<string,any>; artistId?:string; artistName?:string; artistSlug?:string; albumId?:string; albumTitle?:string; albumSlug?:string; genre?:string; trackNumber?:number; isrc?:string; releaseDate?:string; purchasable?:boolean; promotional?:boolean; privateFilePath?:string; streamFilePath?:string; previewUrl?:string; coverImageUrl?:string; [key:string]:any };

type RelationForm = { artistId:string; albumId:string; genre:string; trackNumber:string; isrc:string; releaseDate:string; purchasable:boolean; promotional:boolean };

const emptyForm = { primary:'', slug:'', description:'', status:'draft', price:'0.99', featured:false, details:'{}' };
const emptyRelations:RelationForm = { artistId:'', albumId:'', genre:'', trackNumber:'', isrc:'', releaseDate:'', purchasable:true, promotional:false };
const fallbackArtists:RecordData[] = builtInArtists.map(artist=>({ id:artist.id,name:artist.name,slug:artist.slug,genre:artist.genre,status:'published',description:artist.desc }));

function makeSlug(value:string){return value.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');}
function safeFileName(value:string){return value.toLowerCase().replace(/[^a-z0-9.]+/g,'-').replace(/^-|-$/g,'');}
function writeAscii(view:DataView,offset:number,value:string){for(let index=0;index<value.length;index+=1)view.setUint8(offset+index,value.charCodeAt(index));}
function createWavBlob(buffer:AudioBuffer,seconds=40){const frames=Math.min(buffer.length,Math.floor(buffer.sampleRate*seconds));const channels=Math.min(buffer.numberOfChannels,2);const dataSize=frames*channels*2;const arrayBuffer=new ArrayBuffer(44+dataSize);const view=new DataView(arrayBuffer);writeAscii(view,0,'RIFF');view.setUint32(4,36+dataSize,true);writeAscii(view,8,'WAVE');writeAscii(view,12,'fmt ');view.setUint32(16,16,true);view.setUint16(20,1,true);view.setUint16(22,channels,true);view.setUint32(24,buffer.sampleRate,true);view.setUint32(28,buffer.sampleRate*channels*2,true);view.setUint16(32,channels*2,true);view.setUint16(34,16,true);writeAscii(view,36,'data');view.setUint32(40,dataSize,true);const channelData=Array.from({length:channels},(_,index)=>buffer.getChannelData(index));let offset=44;for(let frame=0;frame<frames;frame+=1){for(let channel=0;channel<channels;channel+=1){const sample=Math.max(-1,Math.min(1,channelData[channel][frame]||0));view.setInt16(offset,sample<0?sample*0x8000:sample*0x7fff,true);offset+=2;}}return new Blob([arrayBuffer],{type:'audio/wav'});}
async function buildPreviewFile(file:File,slug:string){const AudioContextClass=window.AudioContext||(window as typeof window&{webkitAudioContext?:typeof AudioContext}).webkitAudioContext;if(!AudioContextClass)throw new Error('This browser cannot create audio previews.');const context=new AudioContextClass();try{const buffer=await context.decodeAudioData(await file.arrayBuffer());return new File([createWavBlob(buffer,40)],`${slug}-preview.wav`,{type:'audio/wav'});}finally{await context.close();}}

export function SongAdminSection(){
 const[items,setItems]=useState<RecordData[]>([]);const[firestoreArtists,setFirestoreArtists]=useState<RecordData[]>([]);const[albums,setAlbums]=useState<RecordData[]>([]);const[form,setForm]=useState(emptyForm);const[relations,setRelations]=useState<RelationForm>(emptyRelations);const[editingId,setEditingId]=useState<string|null>(null);const[saving,setSaving]=useState(false);const[message,setMessage]=useState('');const[uploading,setUploading]=useState<Record<string,number>>({});const[uploadedDetails,setUploadedDetails]=useState<Record<string,any>>({});
 const artists=useMemo(()=>{const map=new Map<string,RecordData>();fallbackArtists.forEach(a=>map.set(String(a.slug||a.id),a));firestoreArtists.forEach(a=>map.set(String(a.slug||a.id),a));return[...map.values()].sort((a,b)=>String(a.name||a.title).localeCompare(String(b.name||b.title)));},[firestoreArtists]);
 useEffect(()=>onSnapshot(collection(firestore,'songs'),snap=>setItems(snap.docs.map(item=>({id:item.id,...item.data()} as RecordData))),error=>setMessage(`Unable to load songs: ${error.message}`)),[]);
 useEffect(()=>onSnapshot(collection(firestore,'artists'),snap=>setFirestoreArtists(snap.docs.map(item=>({id:item.id,...item.data()} as RecordData))),()=>setFirestoreArtists([])),[]);
 useEffect(()=>onSnapshot(collection(firestore,'albums'),snap=>setAlbums(snap.docs.map(item=>({id:item.id,...item.data()} as RecordData))),()=>setAlbums([])),[]);
 const selectedArtist=artists.find(a=>a.id===relations.artistId);const availableAlbums=albums.filter(album=>!relations.artistId||album.artistId===relations.artistId||album.details?.artistId===relations.artistId);
 const toggleStyle:React.CSSProperties={display:'flex',alignItems:'center',gap:12,width:'100%',padding:'10px 0',margin:0};
 function resetForm(){setEditingId(null);setForm(emptyForm);setRelations(emptyRelations);setUploadedDetails({});setUploading({});setMessage('');}
 function startEdit(item:RecordData){const details=item.details||{};setEditingId(item.id);setUploadedDetails({...details,...(item.privateFilePath?{privateFilePath:item.privateFilePath}:{}),...(item.streamFilePath?{streamFilePath:item.streamFilePath}:{}),...(item.previewUrl?{previewUrl:item.previewUrl}:{}),...(item.coverImageUrl?{coverImageUrl:item.coverImageUrl}:{})});setForm({primary:String(item.title||''),slug:String(item.slug||''),description:String(item.description||''),status:String(item.status||'draft'),price:String(item.price??'0.99'),featured:Boolean(item.featured),details:JSON.stringify(details,null,2)});setRelations({artistId:String(item.artistId||details.artistId||''),albumId:String(item.albumId||details.albumId||''),genre:String(item.genre||details.genre||''),trackNumber:String(item.trackNumber||details.trackNumber||''),isrc:String(item.isrc||details.isrc||''),releaseDate:String(item.releaseDate||details.releaseDate||''),purchasable:item.purchasable!==false&&details.purchasable!==false,promotional:Boolean(item.promotional||details.promotional)});}
 function uploadToStorage(path:string,file:File,key:string,returnPath=false){setUploading(current=>({...current,[key]:0}));return new Promise<string>((resolve,reject)=>{const task=uploadBytesResumable(ref(firebaseStorage,path),file,{contentType:file.type||undefined});task.on('state_changed',snapshot=>setUploading(current=>({...current,[key]:Math.round(snapshot.bytesTransferred/snapshot.totalBytes*100)})),reject,async()=>{const value=returnPath?path:await getDownloadURL(task.snapshot.ref);setUploading(current=>{const next={...current};delete next[key];return next;});resolve(value);});});}
 async function uploadArtwork(file:File){const slug=form.slug.trim()||makeSlug(form.primary||'song');const path=`public/songs/covers/${slug}-${Date.now()}-${safeFileName(file.name)}`;try{const coverImageUrl=await uploadToStorage(path,file,'coverImageUrl');setUploadedDetails(current=>({...current,coverImageUrl}));setMessage('Artwork uploaded.');}catch(error){setMessage(error instanceof Error?error.message:'Artwork upload failed.');}}
 async function uploadMaster(file:File){if(!relations.artistId){setMessage('Select an artist before uploading the WAV master.');return;}const isWav=/\.wav$/i.test(file.name)||['audio/wav','audio/x-wav','audio/wave'].includes(file.type);if(!isWav){setMessage('Upload the original WAV master only. Aureon creates the streaming and preview files automatically.');return;}const slug=form.slug.trim()||makeSlug(form.primary||'song');const artistFolder=String(selectedArtist?.slug||makeSlug(String(selectedArtist?.name||'unassigned')));const stamp=Date.now();const wavFile=file.type==='audio/wav'?file:new File([file],file.name,{type:'audio/wav'});
  try{setMessage('Preparing 256 kbps AAC subscriber stream…');const[streamFile,previewFile]=await Promise.all([buildAacStreamingFile(wavFile,slug),buildPreviewFile(wavFile,slug)]);setMessage('Uploading protected WAV master…');const privateFilePath=await uploadToStorage(`private/full-tracks/${artistFolder}/${slug}-${stamp}-master.wav`,wavFile,'privateFilePath',true);setMessage('Uploading protected AAC subscriber stream…');const streamFilePath=await uploadToStorage(`private/streams/${artistFolder}/${slug}-${stamp}-stream.aac`,streamFile,'streamFilePath',true);setMessage('Uploading 40-second public preview…');const previewUrl=await uploadToStorage(`public/previews/${artistFolder}/${slug}-${stamp}-preview.wav`,previewFile,'previewUrl');setUploadedDetails(current=>({...current,privateFilePath,masterFormat:'WAV',streamFilePath,streamFormat:AUREON_STREAM_FORMAT,streamBitrateKbps:256,previewUrl,previewDuration:40}));setMessage('WAV master secured. AAC stream and 40-second preview created successfully.');}catch(error){setMessage(error instanceof Error?error.message:'Song processing failed.');}}
 async function saveItem(event:React.FormEvent){event.preventDefault();if(!form.primary.trim())return setMessage('Song title is required.');if(!relations.artistId)return setMessage('Please select an artist.');if(!editingId&&(!uploadedDetails.privateFilePath||!uploadedDetails.streamFilePath||!uploadedDetails.previewUrl))return setMessage('Upload the WAV master and wait for Aureon to finish creating the stream and preview before saving.');setSaving(true);try{const manual=JSON.parse(form.details||'{}');const artist=artists.find(item=>item.id===relations.artistId);const album=albums.find(item=>item.id===relations.albumId);const relationData={artistId:artist?.id||'',artistName:String(artist?.name||artist?.title||''),artistSlug:String(artist?.slug||''),genre:relations.genre,releaseDate:relations.releaseDate,albumId:album?.id||'',albumTitle:String(album?.title||''),albumSlug:String(album?.slug||''),trackNumber:Number(relations.trackNumber||0),isrc:relations.isrc,purchasable:relations.purchasable,promotional:relations.promotional};const details={...manual,...uploadedDetails,...relationData};const payload:Record<string,any>={title:form.primary.trim(),slug:form.slug.trim()||makeSlug(form.primary),description:form.description.trim(),status:form.status,featured:form.featured,price:Number(form.price||0),details,...uploadedDetails,...relationData,updatedAt:serverTimestamp()};if(editingId)await updateDoc(doc(firestore,'songs',editingId),payload);else await addDoc(collection(firestore,'songs'),{...payload,createdAt:serverTimestamp()});setMessage('Song saved successfully.');resetForm();}catch(error){setMessage(error instanceof Error?error.message:'Unable to save song.');}finally{setSaving(false);}}
 async function removeItem(id:string){if(confirm('Delete this song record permanently?'))await deleteDoc(doc(firestore,'songs',id));}
 async function togglePublish(item:RecordData){if(item.status!=='published'&&!String(item.streamFilePath||item.details?.streamFilePath||'').startsWith('private/streams/')){setMessage('This song has no compressed subscriber stream. Upload the WAV master again before publishing it.');return;}await updateDoc(doc(firestore,'songs',item.id),{status:item.status==='published'?'draft':'published',updatedAt:serverTimestamp()});}
 return <AdminShell><div className="admin-page-heading"><p className="admin-kicker">Aureon Control Center</p><h1>Songs</h1><p>Upload one WAV master. Aureon stores the original privately, creates a 256 kbps AAC subscriber stream, and preserves the 40-second public preview.</p></div>{message&&<div className="admin-cms-message">{message}</div>}<section className="admin-cms-grid"><form className="admin-cms-form" onSubmit={saveItem}><h2>{editingId?'Edit':'Create'} Song</h2><label>Song title<input value={form.primary} onChange={event=>setForm({...form,primary:event.target.value})}/></label><label>Artist<select required value={relations.artistId} onChange={event=>setRelations({...relations,artistId:event.target.value,albumId:''})}><option value="">Select artist</option>{artists.map(artist=><option key={artist.id} value={artist.id}>{String(artist.name||artist.title)}</option>)}</select></label><label>Album (optional)<select disabled={!relations.artistId} value={relations.albumId} onChange={event=>setRelations({...relations,albumId:event.target.value})}><option value="">{!relations.artistId?'Select an artist first':availableAlbums.length?'Single / no album':'No albums yet — save as single'}</option>{availableAlbums.map(album=><option key={album.id} value={album.id}>{String(album.title||'Untitled album')}</option>)}</select></label><div className="checkout-fields two-columns"><label>Genre<input value={relations.genre} onChange={event=>setRelations({...relations,genre:event.target.value})}/></label><label>Release date<input type="date" value={relations.releaseDate} onChange={event=>setRelations({...relations,releaseDate:event.target.value})}/></label></div><div className="checkout-fields two-columns"><label>Track number<input type="number" min="0" value={relations.trackNumber} onChange={event=>setRelations({...relations,trackNumber:event.target.value})}/></label><label>ISRC (optional)<input value={relations.isrc} onChange={event=>setRelations({...relations,isrc:event.target.value.toUpperCase()})}/></label></div><label style={toggleStyle}><input style={{width:18,height:18,margin:0,flex:'0 0 auto'}} type="checkbox" checked={relations.purchasable} onChange={event=>setRelations({...relations,purchasable:event.target.checked})}/><span>Available to purchase</span></label><label style={toggleStyle}><input style={{width:18,height:18,margin:0,flex:'0 0 auto'}} type="checkbox" checked={relations.promotional} onChange={event=>setRelations({...relations,promotional:event.target.checked})}/><span>Promotional / free song</span></label><label>URL slug<input value={form.slug} placeholder="created-automatically" onChange={event=>setForm({...form,slug:event.target.value})}/></label><label>Description<textarea value={form.description} onChange={event=>setForm({...form,description:event.target.value})}/></label><label>Price (€)<input type="number" min="0" step="0.01" value={form.price} onChange={event=>setForm({...form,price:event.target.value})}/></label><fieldset><legend>Files and media</legend><div style={{marginBottom:18}}><label>Song cover artwork<input type="file" accept="image/*" onChange={async event=>{const file=event.target.files?.[0];if(file)await uploadArtwork(file);}}/></label>{typeof uploading.coverImageUrl==='number'&&<progress value={uploading.coverImageUrl} max={100} style={{width:'100%'}}/>}{uploadedDetails.coverImageUrl&&<small>✓ Artwork uploaded</small>}</div><div style={{marginBottom:18}}><label>Original WAV master — Aureon automatically creates subscriber AAC + 40-second preview<input type="file" accept="audio/wav,audio/x-wav,.wav" onChange={async event=>{const file=event.target.files?.[0];if(file)await uploadMaster(file);}}/></label>{['privateFilePath','streamFilePath','previewUrl'].map(key=>typeof uploading[key]==='number'?<progress key={key} value={uploading[key]} max={100} style={{width:'100%'}}/>:null)}{uploadedDetails.privateFilePath&&<small style={{display:'block'}}>✓ Private WAV master</small>}{uploadedDetails.streamFilePath&&<small style={{display:'block'}}>✓ Private {AUREON_STREAM_FORMAT} subscriber stream</small>}{uploadedDetails.previewUrl&&<small style={{display:'block'}}>✓ Public 40-second preview</small>}</div></fieldset><details><summary>Advanced content fields (optional)</summary><textarea rows={8} value={form.details} onChange={event=>setForm({...form,details:event.target.value})}/></details><label>Status<select value={form.status} onChange={event=>setForm({...form,status:event.target.value})}><option value="draft">Draft</option><option value="published">Published</option></select></label><label style={toggleStyle}><input style={{width:18,height:18,margin:0,flex:'0 0 auto'}} type="checkbox" checked={form.featured} onChange={event=>setForm({...form,featured:event.target.checked})}/><span>Feature on homepage</span></label><button className="primary-button" disabled={saving||Object.keys(uploading).length>0}>{saving?'Saving…':editingId?'Update':'Save'}</button>{editingId&&<button type="button" onClick={resetForm}>Cancel</button>}</form><div className="admin-table-wrap"><table><thead><tr><th>Name</th><th>Status</th><th>Price</th><th>Audio</th><th>Actions</th></tr></thead><tbody>{items.length?items.map(item=>{const stream=String(item.streamFilePath||item.details?.streamFilePath||'');return <tr key={item.id}><td>{String(item.title||'Untitled')}</td><td>{String(item.status||'—')}</td><td>€{Number(item.price||0).toFixed(2)}</td><td>{stream.startsWith('private/streams/')?'AAC ready':'Legacy master stream'}</td><td><button type="button" onClick={()=>startEdit(item)}>Edit</button><button type="button" onClick={()=>togglePublish(item)}>{item.status==='published'?'Unpublish':'Publish'}</button><button type="button" onClick={()=>removeItem(item.id)}>Delete</button></td></tr>}):<tr><td colSpan={5}>No songs yet.</td></tr>}</tbody></table></div></section></AdminShell>;
}
